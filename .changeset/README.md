# Changesets

Run `pnpm changeset` after changing `packages/catalog` or `packages/cli` and
describe the change. That file becomes the changelog entry and drives the
version bump. See https://github.com/changesets/changesets for details.
