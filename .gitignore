node_modules/
dist/
.next/
out/
*.tsbuildinfo
.env
.env.local
.DS_Store
coverage/
.vercel

# Regenerated by `next dev`/`next build` every run (see the file itself).
# Not ours to author, so not ours to commit or copy-check.
apps/web/AGENTS.md
apps/web/CLAUDE.md
